# =============================================================================
# Python Dependencies for Bar Hopping Optimizer Backend
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.2

# Data Processing  
pandas==2.1.3
openpyxl==3.1.2
dateparser==1.2.0

# Configuration
python-dotenv==1.0.0

# HTTP Client (for API calls)
httpx==0.25.2

# Production Server
gunicorn==21.2.0

# =============================================================================
# ML/LLM Dependencies - ONLY INSTALL ON LINUX SERVER WITH GPU
# =============================================================================
# These packages are installed automatically by Railway during deployment.
# DO NOT install locally on Mac/Windows - they won't work without CUDA GPU.
#
# If you need to test with the LLM locally on a Linux machine with GPU, 
# uncomment these lines:
#
# torch>=2.0.0
# transformers==4.46.3
# accelerate==0.34.2
# peft==0.13.2
# bitsandbytes>=0.42.0
# sentencepiece>=0.1.99
# =============================================================================
